# 原理
gsplat 的 distributed=True 分布式渲染原理是将渲染任务分配到多个 GPU 上，每个 GPU 都渲染完整的图像，只是各自只算一部分高斯，最后通过通信合起来。
# 解决的问题
只解决超大场景 显存爆炸。
不解决推理速度
# 实验结果
单卡3路camera渲染时间：56ms左右
3卡3路camera渲染时间：99ms左右
多卡的推理时间比单卡还要慢，可能是通信开销导致的。
# 结论
gsplat distributed = 显存分摊器，不是速度加速器
想要加速推理渲染只能自己做数据并行，也就是调度层下功夫